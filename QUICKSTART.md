# StudyFlow - Быстрый старт

## ✅ Текущее состояние проекта

### Реализовано:
- ✓ Модели данных (Task, Subject, Stats)
- ✓ Полный UI для 6 основных экранов
- ✓ Bottom Navigation с переключением вкладок
- ✓ Mock данные для демонстрации
- ✓ Красивый дизайн с Material Design 3
- ✓ Цветовая дифференциация предметов
- ✓ Структура проекта готова

### Добавлено:
- Промты для генерации дизайна в Midjourney/DALL-E
- Примеры REST API сервиса
- Примеры Hive локального хранилища
- Подробное руководство разработки

---

## 📦 Установка и запуск

### 1. Установка зависимостей
```bash
cd study
flutter pub get
```

### 2. Локализация (для támporte русского языка в календаре)
```bash
flutter pub add intl
```

### 3. Запуск приложения
```bash
# На эмуляторе/девайсе
flutter run

# С включением горячей перезагрузки
flutter run -d <device_id>

# Web версия
flutter run -d chrome
```

### 4. Сборка для релиза
```bash
# Android
flutter build apk --release

# iOS
flutter build ios --release

# Web
flutter build web --release
```

---

## 🎨 Использование дизайн-промтов

### Для генерации UI дизайна:

**Вариант 1: Быстро с Midjourney (рекомендуется)**
```
Скопируй краткий промт из UI_DESIGN_PROMPTS.md
Отправить в Discord канал Midjourney
Дождись результата (5-10 минут)
Используй для вдохновения при улучшении существующего UI
```

**Вариант 2: DALL-E 3 на ChatGPT Plus**
```
1. Копируй полный промт из UI_DESIGN_PROMPTS.md
2. Вставь в ChatGPT
3. Запроси генерацию изображений
4. Скачай высокое разрешение
```

**Вариант 3: Figma макет**
```
Используй Figma для создания высокофидельного макета на основе нашего UI
Скачай компоненты для Flutter
```

---

## 🚀 Следующие шаги разработки

### Фаза 1: Основная функциональность (1-2 недели)

#### 1.1 Локальное хранилище
```bash
flutter pub add hive
flutter pub add hive_flutter
flutter pub dev add hive_generator
flutter pub dev add build_runner

# Генерация кода
flutter pub run build_runner build
```

Код уже подготовлен в `lib/services/storage_service.dart`

#### 1.2 Управление состоянием (выбрать один вариант)

**Вариант A: Riverpod (рекомендуется для новых проектов)**
```bash
flutter pub add flutter_riverpod
flutter pub add riverpod_generator
flutter pub dev add build_runner riverpod_generator
```

**Вариант B: Provider**
```bash
flutter pub add provider
```

**Вариант C: BLoC**
```bash
flutter pub add flutter_bloc
flutter pub add bloc
```

#### 1.3 Экраны создания/редактирования задач
Создать:
- `lib/features/tasks/task_create_page.dart` - создание задачи
- `lib/features/tasks/task_detail_page.dart` - детали задачи
- `lib/features/tasks/task_edit_page.dart` - редактирование

#### 1.4 Тестирование
```bash
# Unit тесты
flutter test test/models/task_model_test.dart

# Widget тесты
flutter test test/widgets/task_card_test.dart
```

### Фаза 2: API интеграция (2-3 недели)

#### 2.1 REST API сервис
```bash
flutter pub add http
flutter pub add dio  # альтернатива http
```

Код уже подготовлен в `lib/services/api_service.dart`

#### 2.2 Аутентификация
- JWT токены для безопасности
- Хранение в secure storage
- Refresh token механизм

```bash
flutter pub add flutter_secure_storage
```

#### 2.3 Синхронизация data
- Оффлайн кэширование
- Sync при подключении к интернету
- Конфликт разрешение

### Фаза 3: Push-уведомления (1-2 недели)

#### 3.1 Локальные уведомления
```bash
flutter pub add flutter_local_notifications
```

Код уже запраздготовлен в `lib/services/notification_service.dart` (используй вместо Firebase)

#### 3.2 Удалённые уведомления (выбрать один)

**Вариант A: OneSignal (рекомендуется)**
```bash
flutter pub add onesignal_flutter
```

**Вариант B: Собственный server с FCM**
```bash
flutter pub add firebase_messaging  # если решишь вернуть Firebase только для этого
```

### Фаза 4: Дополнительные функции (2-3 недели)

#### 4.1 Календарь устройства
```bash
flutter pub add device_calendar
```

#### 4.2 Анализ и аналитика
```bash
flutter pub add mixpanel_flutter
# или
flutter pub add amplitude_flutter
```

#### 4.3 Экспорт данных
- PDF экспорт
- CSV экспорт

```bash
flutter pub add pdf
flutter pub add csv
```

### Фаза 5: Полировка и оптимизация (1-2 недели)

- [ ] Тёмный режим
- [ ] Интернационализация (i18n)
- [ ] Анимации
- [ ] Адаптивный дизайн (планшет)
- [ ] Оптимизация производительности
- [ ] Crash reporting (Sentry)

```bash
flutter pub add adaptive_theme  # для тёмного режима
flutter pub add easy_localization  # для переводов
```

---

## 📋 Простые задачи для начала

### Задача 1: Реализовать создание задачи (30 мин)
```dart
// 1. Создать TaskCreatePage с формой
// 2. Добавить валидацию форму
// 3. Сохранить в StorageService
// 4. Обновить список задач
```

### Задача 2: Подключить Hive (1 час)
```dart
// 1. Инициализировать StorageService при старте
// 2. Загружать данные из Hive вместо mock
// 3. Сохранять при создании/обновлении задачи
// 4. Использовать Hive как источник истины
```

### Задача 3: Добавить фильтрацию дат (30 мин)
```dart
// 1. Добавить фильтр "Сегодня" / "На неделю" / "На месяц"
// 2. Использовать extension методы для дат
// 3. Обновить список задач при смене фильтра
```

### Задача 4: Реализовать тёмный режим (1 час)
```dart
// 1. Создать отдельный ThemeData для тёмного режима
// 2. Добавить toggle в Settings
// 3. Сохранить предпочтение в SharedPreferences или Hive
// 4. Применить при загрузке приложения
```

### Задача 5: Добавить push-уведомления (1.5 часа)
```dart
// 1. Инициализировать flutter_local_notifications
// 2. Запрашивать разрешения на уведомления
// 3. Создавать напоминание за 1 час до дедлайна
// 4. Показывать при разблокировке экрана
```

---

## 🧪 Тестирование

### Unit тесты
```bash
# Запуск всех тестов
flutter test

# Конкретный тест
flutter test test/models/task_model_test.dart

# С покрытием
flutter test --coverage
```

### Widget тесты
```dart
testWidgets('Task card displays correctly', (WidgetTester tester) async {
  await tester.pumpWidget(const TestApp(
    home: TaskCard(task: mockTask, subject: mockSubject),
  ));
  expect(find.text('Решить задачи по математике'), findsOneWidget);
});
```

### Integration тесты
```bash
flutter drive --target=test_driver/app.dart
```

---

## 📚 Полезные ресурсы

### Документация
- [Flutter Official](https://flutter.dev)
- [Material Design 3](https://material.io/design)
- [Dart Docs](https://dart.dev/guides)

### Учебные материалы
- [Flutter Tutorial](https://www.youtube.com/playlist?list=PL-E0-K5LzKHbJwJJZ_B3zPQVHjkPFAG2J)
- [Riverpod Guide](https://riverpod.dev/docs/introduction)
- [Hive Database](https://docs.hivedb.dev/)

### График разработки
```
Неделя 1: Storage + Create Task Screen
Неделя 2: API Integration + Authentication
Неделя 3: Push Notifications
Неделя 4: Calendar Sync + Analytics
Неделя 5: Polish + Testing + Optimization
Неделя 6: Beta Testing + Fixes
```

---

## 🐛 Решение проблем

### Проблема: Ошибка "The build context used to pop or push routes"
**Решение**: Убедись, что навигация происходит после frame:
```dart
WidgetsBinding.instance.addPostFrameCallback((_) {
  Navigator.push(...);
});
```

### Проблема: Hot reload не работает
**Решение**: Перезапусти приложение
```bash
flutter run
# или
flutter run --verbose
```

### Проблема: Hive ошибка при инициализации
**Решение**: Убедись, что `init()` вызывается до использования:
```dart
await storageService.init();
```

### Проблема: Дубликаты в списке задач
**Решение**: Используй `.toSet()` или проверь уникальность ID при добавлении

---

## 📞 Контакты и поддержка

Если при разработке возникают вопросы:
1. Проверь документацию пакета
2. Посмотри примеры на GitHub
3. Используй AI помощника (ChatGPT, Copilot)
4. Поищи в Stack Overflow

---

## ✨ Tips & Tricks

### Быстрая навигация
```dart
// Вместо повторяющегося кода
context.push('/tasks');

// Используй extension:
extension BuildContextX on BuildContext {
  void goHome() => go('/');
  void goTasks() => go('/tasks');
}
```

### Переиспользование виджетов
```dart
// Создавай component library
lib/
├── widgets/
│   ├── common/
│   │   ├── app_button.dart
│   │   ├── app_card.dart
│   │   └── app_text_field.dart
│   └── domain/
│       ├── task_card.dart
│       └── subject_card.dart
```

### Управление состоянием
```dart
// Используй Riverpod для простоты:
final taskCountProvider = Provider((ref) {
  return ref.watch(allTasksProvider).length;
});

// Автоматически обновляется когда allTasksProvider меняется
```

---

## 🎯 Давай кодить! 🚀

Начни с малого:
1. Запусти приложение: `flutter run`
2. Пройдись по всем экранам
3. Выбери одну функцию для реализации
4. Создай файл, напиши код, протестируй
5. Коммит в git
6. Переходи к следующей функции

**Всё готово! Начинай разработку! 💪**
